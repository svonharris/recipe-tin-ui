Run `git diff --staged` to find all currently staged files. If nothing is staged, stop and let me know — do not stage anything.

Run `git status` and `git diff --staged` to show what is staged.

Based on the staged diff, draft a concise, descriptive commit message explaining what changed. Do NOT include a Co-Authored-By line.

Present the proposed commit message to me and wait for my explicit approval before committing. If I reject or ask for changes, revise and ask again.

Once I approve:
1. Commit the staged files with the approved message — no Co-Authored-By line
2. Push the commit to the remote repo
